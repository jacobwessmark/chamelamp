# CMake configuration for Chamelamp project
# Minimum required version and project configuration
cmake_minimum_required(VERSION 3.5)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(chamelamp)